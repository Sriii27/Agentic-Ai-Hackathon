# Care Mediator frontend (Next.js) — this is the root app.
#
# Optional: only needed if the backend isn't running on the default
# http://localhost:4000 (e.g. a different port or a deployed backend URL).
# NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
